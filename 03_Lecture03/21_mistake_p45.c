#include <stdio.h>

int main(void)
{
    int a, b;
    float x, y;
    a = 5;
    10 = x;//константа
    y = 7,8; //точка вместо запятой
    b = 2.5; //неверный тип
    a = b + x; //пропадет дробная часть
}
